import React from "react";

const STATE_LOADING = 0;
const STATE_REDIRECT = 1;
const STATE_RENDER = 2;

const GuardedRouteComponent = ({component: Component,...rest}) => {

};

export default GuardedRouteComponent;